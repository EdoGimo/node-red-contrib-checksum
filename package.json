{
	"name": "node-red-contrib-checksum",
	"version": "1.0.1",
	"description": "Calculates the checksum of the payload (using crypto-js) and compares it with the one given to it (msg.checksum). \n Returns a boolean if the checksum is correct (true) or wrong (false). Returns a string if one of the two properties is missing.\n Supports the elaboration of strings or files.",
    "dependencies": {
		"crypto-js":"4.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/edogimo/node-red-contrib-checksum.git"
	  },
	"keywords": ["node-red", "checksum", "crypto-js", "sha256", "sha1", "md5", "sha3"],
	"node-red": {
		"nodes": {
			"Checksum":"checksum/checksum.js"
		}
	}
}